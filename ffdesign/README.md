
  # Flyer prototype with hover animation

  This is a code bundle for Flyer prototype with hover animation. The original project is available at https://www.figma.com/design/txgOxJBO65uXzmXOdCfUdO/Flyer-prototype-with-hover-animation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  